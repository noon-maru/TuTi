import { View, Text } from "react-native";

const SponserScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Sponser Screen</Text>
    </View>
  );
};

export default SponserScreen;
